name: Test Add-on Image

on:
  pull_request:
    paths:
      - "seerr/**"
      - ".github/workflows/test-addon-image.yml"
  push:
    branches:
      - main
    paths:
      - "seerr/**"
      - ".github/workflows/test-addon-image.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: test-addon-image-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build ${{ matrix.arch }} image
    runs-on: ubuntu-latest
    timeout-minutes: 30

    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            platform: linux/amd64
            load: true
            smoke_test: true
          - arch: aarch64
            platform: linux/arm64
            load: false
            smoke_test: false

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build add-on image
        uses: docker/build-push-action@v6
        with:
          context: ./seerr
          file: ./seerr/Dockerfile
          platforms: ${{ matrix.platform }}
          push: false
          load: ${{ matrix.load }}
          tags: addon-seerr:${{ matrix.arch }}-test
          build-args: |
            BUILD_ARCH=${{ matrix.arch }}
            BUILD_DATE=${{ github.event.repository.updated_at }}
            BUILD_DESCRIPTION=Seerr Home Assistant app
            BUILD_NAME=Seerr
            BUILD_REF=${{ github.sha }}
            BUILD_REPOSITORY=${{ github.repository }}
            BUILD_VERSION=dev

      - name: Inspect built image
        if: matrix.smoke_test
        shell: bash
        run: |
          set -Eeuo pipefail

          docker image inspect \
            "addon-seerr:${{ matrix.arch }}-test" \
            >/dev/null

          docker run --rm \
            --entrypoint /bin/bash \
            "addon-seerr:${{ matrix.arch }}-test" \
            -c '
              set -Eeuo pipefail

              test -f /opt/package.json
              test -f /opt/pnpm-lock.yaml
              test -f /opt/dist/index.js
              test -d /opt/.next
              test -d /opt/node_modules
              test -x /etc/s6-overlay/s6-rc.d/seerr/run

              command -v node
              command -v pnpm

              node --version
              pnpm --version
            '

      - name: Start and test add-on container
        if: matrix.smoke_test
        shell: bash
        run: |
          set -Eeuo pipefail
      
          CONFIG_DIR="$(mktemp -d)"
          CONTAINER_NAME="addon-seerr-smoke-test"

          docker run \
            --detach \
            --name "${CONTAINER_NAME}" \
            --publish 5055:5055 \
            --volume "${CONFIG_DIR}:/config" \
            --env NODE_ENV=production \
            --env PORT=5055 \
            --env CONFIG_DIRECTORY=/config \
            --workdir /opt \
            --entrypoint /bin/bash \
            "addon-seerr:${{ matrix.arch }}-test" \
            -c 'exec pnpm start'

          echo "Waiting for Seerr to become available..."

          for attempt in $(seq 1 60); do
            if ! docker inspect \
              --format '{{.State.Running}}' \
              "${CONTAINER_NAME}" \
              | grep -qx true; then
              echo "Container stopped before Seerr became ready." >&2
              exit 1
            fi

            if curl \
              --fail \
              --silent \
              --show-error \
              --output /dev/null \
              http://127.0.0.1:5055/; then
              echo "Seerr responded successfully."
              break
            fi

            if [[ "${attempt}" -eq 60 ]]; then
              echo "Seerr did not become available within 120 seconds." >&2
              exit 1
            fi

            sleep 2
          done

          sleep 5

          docker inspect \
            --format '{{.State.Running}}' \
            "${CONTAINER_NAME}" \
            | grep -qx true

          if docker logs "${CONTAINER_NAME}" 2>&1 \
            | grep -q 'EADDRINUSE'; then
            echo "Detected duplicate Seerr process or port conflict." >&2
            exit 1
          fi

          if docker logs "${CONTAINER_NAME}" 2>&1 \
            | grep -q 'Server ready on port 5055'; then
            echo "Verified Seerr startup message."
          else
            echo "Expected Seerr startup message was not found." >&2
            exit 1
          fi
